-r requirements.txt
-r requirements-train.txt  # adds torch, catboost, optuna, shap, mlflow, imbalanced-learn

# Testing
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27  # required by starlette.testclient (FastAPI test client)

# Linting
ruff>=0.4

# Type checking
mypy>=1.10
pandas-stubs>=2.2

# Security
pip-audit>=2.7
bandit>=1.7

# External-benchmark data path (benchmarks/datasets/nyc_rolling_sales_2024.py):
# `requests` downloads the NYC.gov rolling-sales workbooks and `openpyxl` is the
# engine pandas.read_excel needs to parse them. Both were previously undeclared
# — requests resolved transitively through streamlit and openpyxl only via an
# inline `pip install` in benchmark.yml, so a fresh env could import the module
# and fail at download time.
requests==2.34.2
openpyxl==3.1.5
