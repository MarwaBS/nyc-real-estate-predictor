# Training-only dependencies - NOT shipped in the production Docker image.
#
# Split from requirements.txt so the runtime image stays small (~300MB) and the
# CI security scan on requirements.txt does not flag CVEs in packages that
# never run in prod (mlflow carries pending CVEs; none reach the serving path).
#
# Pulled in by requirements-dev.txt so local devs + CI's test job get the full
# toolchain. The CI security job scans requirements.txt only.
#
# torch, catboost, optuna and imbalanced-learn were removed: after the
# multi-task net and the extended training path were deleted, `grep -r` found
# zero imports for any of them. An unused pinned dependency is a CVE surface
# that buys nothing -- torch alone carried two advisories.

-r requirements.txt

# shap: imported by src/models/explain.py for the training-time SHAP pass.
shap==0.52.0

# mlflow: imported by run_training.py for experiment tracking.
mlflow==3.14.0
